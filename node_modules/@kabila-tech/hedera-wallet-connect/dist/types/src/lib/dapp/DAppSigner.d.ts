import { Signer, AccountBalance, AccountId, AccountInfo, Executable, Key, LedgerId, SignerSignature, Transaction, TransactionRecord } from '@hashgraph/sdk';
import type { ISignClient } from '@walletconnect/types';
export declare class DAppSigner implements Signer {
    private readonly accountId;
    readonly topic: string;
    private readonly ledgerId;
    readonly extensionId?: string | undefined;
    static signClient: ISignClient;
    signerAccountId: string;
    /**
     * These are the node accounts that will be utilized for signing transactions.
     */
    nodesAccountIds: AccountId[];
    constructor(accountId: AccountId, topic: string, ledgerId?: LedgerId, extensionId?: string | undefined);
    static initialize(signClient: ISignClient): void;
    /**
     * Set the nodes to sign transactions that require a single signature.
     *
     * @param {AccountId[]} nodesAccountIds
     */
    setNodes(nodesAccountIds: AccountId[]): void;
    request<T>(request: {
        method: string;
        params: any;
    }): Promise<T>;
    getAccountId(): AccountId;
    getAccountKey(): Key;
    getLedgerId(): LedgerId;
    getNetwork(): {
        [key: string]: string | AccountId;
    };
    getMirrorNetwork(): string[];
    getAccountBalance(): Promise<AccountBalance>;
    getAccountInfo(): Promise<AccountInfo>;
    getAccountRecords(): Promise<TransactionRecord[]>;
    sign(data: Uint8Array[], signOptions?: Record<string, any>): Promise<SignerSignature[]>;
    signTransaction<T extends Transaction>(transaction: T): Promise<T>;
    checkTransaction<T extends Transaction>(transaction: T): Promise<T>;
    populateTransaction<T extends Transaction>(transaction: T): Promise<T>;
    call<RequestT, ResponseT, OutputT>(request: Executable<RequestT, ResponseT, OutputT>): Promise<OutputT>;
    executeTransaction<T extends Transaction>(transaction: T): Promise<import("@hashgraph/sdk/lib/transaction/TransactionResponse").TransactionResponseJSON>;
    signMessage(message: string): Promise<{
        signatureMap: string;
    }>;
    signAndExecuteQuery(query: string): Promise<{
        response: string;
    }>;
    signAndExecuteTransaction<T extends Transaction>(transaction: T): Promise<import("@hashgraph/sdk/lib/transaction/TransactionResponse").TransactionResponseJSON>;
}
//# sourceMappingURL=DAppSigner.d.ts.map